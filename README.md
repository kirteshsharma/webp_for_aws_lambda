# webp_for_aws_lambda
the **webp_for_aws_lambda** is convert normal image and `webp` file.
## Usage

**API**

```java
String src  = "a.webp";
String dest = "a.png";
WebpIO.toNormalImage(src, dest);
```

Okay, lets go.
